@* @model List<Website.Models.Products>
@{
    Layout = "~/Views/_Shared/_LayoutTrangChu.cshtml";
}

<section class="ftco-section">
    <div class="container">
        <h4>Kết quả tìm kiếm: "@ViewBag.Key"</h4>

        <div class="row">
            @if (Model.Count > 0)
            {
                foreach (var item in Model)
                {
                    <div class="col-md-3">
                        <div class="product text-center">
                            <a href="/Products/Detail/@item.Id">
                                <img src="~/Upload/Products/@item.Photo"
                                     class="img-fluid">
                            </a>
                            <h3>@item.Name</h3>
                            <p>
                                @string.Format("{0:N0} ₫",
                                item.Price - item.Discount)
                    </p>
                </div>
            </div>
                        }
            }
            else
            {
                <p>❌ Không tìm thấy sản phẩm</p>
            }
        </div>
    </div>
</section> *@
